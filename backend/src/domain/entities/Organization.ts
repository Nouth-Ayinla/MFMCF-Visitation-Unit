export interface Department {
  id: string;
  name: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface Level {
  id: string;
  levelNumber: string;
  createdAt: Date;
  updatedAt: Date;
}
